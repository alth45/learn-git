name: Belajar Github Action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  sapa:
    runs-on: ubuntu-latest
    steps:
      - name: Sapa Dunia
        run: echo "Hallo github action jalan!"

  cek-sistem:
    runs-on: ubuntu-latest
    steps:
      - name: Cek Tanggal
        run: date
      - name: Cek Folder
        run: ls